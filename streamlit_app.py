
import streamlit as st
import numpy as np
import pandas as pd
import re
# import time
st.title('strategy')

    # response = requests.get(url=url, params={'search' : '裂咬'})

    # response.text.split('<div class=\'mw-search-result-heading\'>')

# url = 'https://www.pikalytics.com/pokedex/gen9vgc2023series1'
# response = requests.get(url)
# minlist = response.text.split('min_list')[1].split('span')

# result = []
# for i in minlist:
#     if 'pokemon-name' in i  or 'class="float-right' in i or 'pokemon-search' in i:
#         i = i[i.find('>')+1 : i.rfind('<')]
#         result.append(i)

# result = np.array(result)
# result = pd.DataFrame(result.reshape(-1,3))


